# Python instructions

- Target Python 3.11+
- Use type hints on public functions
- Prefer `pathlib.Path` over string paths
- Keep I/O (subprocess) at module edges (`ffmpeg.py`)
- Run `ruff check` and `pytest` before committing
